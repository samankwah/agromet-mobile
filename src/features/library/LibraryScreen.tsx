import { ComingSoon } from '../../shared/ui/ComingSoon';

/** ComingSoon stub — the Bulletin Library, Maps, and Account/Settings
 * milestones build bulletins/news/maps/FAQs/contacts/settings here.
 * Domain models (Bulletin, MapLayer, UserSettings, Account) and their
 * services already exist so those milestones are pure UI work. */
export function LibraryScreen() {
  return <ComingSoon title="Library" message="Bulletins, maps, FAQs, contacts, and settings are coming in a future update." />;
}
